# Guidance-audit scoreboard

> Generated by `./gradlew guidanceStatus` (`scripts/guidance_status.py`) from commit `415f2926`. Do not hand-edit -- every number is derived from the linked raw artifact and re-derived on each run.

## At a glance

| # | Metric | Value | Source artifact |
|---|---|---|---|
| 1 | corpus-pass-rate | **226/226** (100.0%); calibration+ratchet gate green | [`docs/guidance-audit/guidance-config-audit.json`](guidance-config-audit.json) + ratchet JUnit |
| 2 | open trustworthy findings | **0** open (); 7 shipped of 8 | [`docs/guidance-audit/findings-guidance-config.md`](findings-guidance-config.md) |
| 3 | acceptance-gate % | **8/8** (100.0%) full pass | [`docs/guidance-audit/acceptance-runs/2026-06-09.md`](acceptance-runs/2026-06-09.md) |
| 4 | guidance-drive confidence % | **100.0%** item-weighted (226/226 sources PASS); gate 95% PASS | [`docs/guidance-audit/acceptance-runner-report.json`](acceptance-runner-report.json) |

## 1. Corpus-pass-rate

Of **226** sources, **226** carry no REAL guidance-config finding. The static analyzer (`scripts/audit_guidance_config.py`) emits 186 raw findings; filtering to the REAL data classes (C1/N1 can-never-fire, C2 earlier-target loop-never) leaves **0** real instances across **0** sources (the rest are by-design -- E4 self-loop markers, the Shades recurring-gather MANUAL, and highlight-only/cosmetic hits -- the #775 lesson: an artifact is never a bug).

| Detector | Raw findings |
|---|---|
| D1 | 0 |
| D1b | 11 |
| D2 | 175 |

Trust gates: calibration `--calibrate` **PASS** (re-derives the maintainer's hand-found bug counts); `GuidanceConfigInvariantsRegressionTest` ratchet **PASS** (8 test(s), 0 failures, run 2026-06-10T00:31:21) -- runs under `./gradlew check`.

Real-affected sources:

> 

## 2. Open trustworthy findings

The discriminator's backlog [`docs/guidance-audit/findings-guidance-config.md`](findings-guidance-config.md) holds **8** REAL findings (each mechanism-cited). **0** are still routed to the maintainer (open); **7** have shipped as PRs.

- Open (routed): none
- Shipped: C1, C2, C4, C5, N1, N2, N3

Harness ARTIFACTs are excluded: the latest acceptance run's `ARRIVE_AT_TILE` synthetic-injection quirk (Finding 1) is a test-harness limitation, not a product bug, so it is not counted here.

## 3. Acceptance-gate %

Latest acceptance sweep [`docs/guidance-audit/acceptance-runs/2026-06-09.md`](acceptance-runs/2026-06-09.md) drove a never-driven sample of **8** sources: **8 full PASS** (100.0%), 0 PARTIAL, 0 FAIL.

- PASS: General Graardor, The Nightmare, Zulrah, Barrows, Vorkath, Vet'ion, Skotizo, Catacombs of Kourend
- PARTIAL: none (blocked by the Finding-1 `ARRIVE_AT_TILE` harness quirk, not product bugs)
- FAIL: none (static dead-end -- the C1 unwired-`ACTOR_DEATH` data bug)

## 4. Guidance-drive confidence %

The acceptance-runner ([`docs/guidance-audit/acceptance-runner-report.json`](acceptance-runner-report.json)) drove **226** selected source(s) through the live bridge by synthesizing each step's completion event; **226** drove fully to completion. Item-weighted confidence (clog items in PASS sources / clog items selected) is **100.0%**, which **meets** the `CONFIDENCE_GATE_PCT = 95` threshold.

## Recommendation implied by the board

GREEN -- corpus clean, zero open trustworthy findings, acceptance sample all-pass. Eligible to hand off to resubmission-hygiene (ROADMAP §2).
