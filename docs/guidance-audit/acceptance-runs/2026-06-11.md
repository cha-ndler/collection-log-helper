# In-Game Validation Run — 2026-06-11 (operator + actuation seam, logged in)

> Scope: live-confirm the six Plugin Hub blocker fixes merged this cycle (#801–#806)
> plus the T0.7 privacy fix, on a logged-in dev account driven through the dev-bridge
> actuation seam (synthetic local events, never game input) + real operator travel/combat.
> Every verdict is anchored to a raw `client.log` line, a `state.read` transcript, or a
> screenshot in `docs/screenshots/validation-2026-06-11/`. RSN redacted as `<RSN>`.

- Host: Windows 11; client `gradlew -I harness-init.gradle run` (dev-bridge harness 0.3:
  adds `guidance.advance` and `awaitingRestock` in `state.read`, toolkit #48).
- Capture tooling: `screenshot` / `region_capture` with the new `crop_top` + `crop_chat`
  privacy crops (toolkit #52) — every committed image has the title bar (RSN) and chat
  strip removed at capture time; the reported window title is redacted in tool output.
- Final confirmations ran on an integration build (`master` + the icon-only marker PR #816
  + the manhole data PR #820) so the resized icon and closed-manhole highlight were live.
- **Screenshots are retained local-only** (not committed): several captures are from
  populated areas (Castle Wars lobby, Varrock) where third-party player names can appear in
  the 3D scene even with the chat strip cropped, so per the repo privacy posture the PNGs
  stay on the operator's disk; the `client.log` lines and `state.read` transcripts quoted
  below are the committed evidence. Filenames are listed as local pointers.

## Verdicts

| Row / fix | Verdict | Evidence |
|-----------|---------|----------|
| **T0.7** privacy: no RSN in CLH logs | **PASS** (flips `[!]`→`[x]`) | Full login + TempleOSRS sync + efficiency export window scanned: **0** occurrences of the account name across **10,830** CLH-attributed `client.log` lines (and 0 anywhere in the 11,230-line window). Efficiency export file written (`...\collection-log-helper\<RSN>\efficiency-export.txt`, 230 KB). Redacted phrasing live: `TempleOsrsKcSyncer - Fetching TempleOSRS KC for the current player`, `TempleSyncOrchestrator - ... KC sync complete: 0 sources updated, 7 skipped` (no `for user '<RSN>'` suffix). Closes the #804/PR #807 acceptance. |
| **#801** drop-less completion re-loops the grind | **PASS** | Real drop-less Giant Mole kill (auto-advance on): `Step 3 complete (ACTOR_DEATH: 5779)` → `Guidance sequence complete for Giant Mole` → `CollectionStateChangeHandler - Sequence complete but target slot not unlocked for Giant Mole — re-activating` → `state.read` shows Giant Mole active again at step 3/3 (16:04:27, repeated across several kills). Also reproduced **fully headless** via the #51 seam scenario: master PASS (re-activation receipt); fix commit `28ef2ac3` reverse-applied → FAIL (`rolled to 'Fossil Island Notes'`). |
| **#803** mid-lair re-activation lands on the kill step | **PASS** | Fresh login standing inside the mole lair: `guidance.activate Giant Mole` → `state.read` `currentStepIndex: 2` (kill step 3/3) directly, not the travel step (`803-mid-lair-reactivation-kill-step.png`). Pre-fix this stranded the player on "Travel to Falador Park" once the mole burrowed past the 5-tile default. |
| **#802** collection-log marker on NPC targets | **PASS** | Book marker renders above a **tall** NPC (Giant Mole, `802-mole-icon-only-*.png`) and a **short** one (Lanthus at Castle Wars, `802-lanthus-icon-only-*.png`). Per live review the NPC path was reduced to **icon only** (no floating action text / arrow) and the sprite scaled to 60% so it clears small models (PR #816); marker clearance raised to 90 local height units after the icon overlapped the mole at 40. |
| **#806** Thermonuclear entrance completes inside, not on surface | **PASS** | On the surface a few tiles from the cave: step held at `currentStepIndex: 1` (enter-cave) for 6+ s, entrance object glow + book marker persisted (`806-thermo-surface-glow-persists-*.png`). On entry: `Step 2 complete (ARRIVE_AT_ZONE: player in zone [2350,9400 - 2432,9472] plane 0)` → advanced to navigate (19:11:10). Completed via the underground zone, not surface proximity. Receipt: `806-thermo-entry-receipt.txt`. |
| **#806** Scurrius manhole does not insta-complete | **PASS** | Standing on the surface manhole: `guidance.activate Scurrius` landed on `currentStepIndex: 2` (climb-down) and **held** 6+ s (pre-fix the preceding travel step's radius would have insta-completed it) — `806-scurrius-surface-no-insta-complete-*.png`. On descent: `Step 3 complete (ARRIVE_AT_ZONE: player in zone [3225,9850 - 3325,9890] plane 0)` → advanced to find-portal (19:18:51). |
| **T2.11** recurring-gather highlight persists past first pickup | **PASS (logic)** | On the Shades gather step (MANUAL, `currentStepIndex: 2`), the operator picked up a shade key and deposited it; the step **held at 3/5** with **no** `auto-advancing (skipIfHasAnyItemIds satisfied)` line in the log — the `isRecurringGatherSequence()` suppression (`GuidanceSequencer:596`) fired correctly. Persistence across a second pickup is the identical code path. |
| **T1.4** sync surfaces (TempleOSRS-only) | **partial** | Login auto-sync round-trip clean again: `Auto-sync on login: triggering TempleOSRS KC sync` → `KC sync complete: 0 sources updated, 7 skipped`, in-game toast, no hang. Sub-clause #1 (flags default OFF + RSN warning) code-confirmed: Sync section holds only `enableTempleOsrsSync` (default false, description names the third-party server). Sub-clause #2 (populate-with-data) **blocked**: the dev account has no usable TempleOSRS KC (0/7 again) — needs an RSN with real KC. #4/#6 remain needs-human. |

## Issues filed from this session

Quality/data issues surfaced during the live run (none were Plugin Hub blockers; all are
follow-ups). Consumer repo: #817 (guidance tooltip overflows in fixed mode), #818 (Slayer
helmet not accepted for the Gas mask requirement), #819 → fixed by **PR #820** (manhole not
highlighted while closed), #821 (Shades gather step highlights a "reward pillar" instead of
the key), #822 (recurring-gather MANUAL steps read as "stuck" — add a "press Next" hint).

Tooling: each fix class was turned into a corpus-completeness detector so a future
autonomous session catches every straggler — toolkit #58 (umbrella: the no-straggler
principle) + #59 (functional-equivalent item requirements), #60 (closed/open-cover entrance
objects), #61 (entrance steps completing on surface proximity), #62 (over-long descriptions).

## Net

The six merged blockers (#801–#806) and the T0.7 privacy gate are all **live-confirmed PASS**.
T2.11 is logic-confirmed. T1.4 #2 remains blocked on a TempleOSRS-populated account.
